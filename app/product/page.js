import Page from '../../components/Page';
export default function Product(){return <Page eyebrow="Product" title="From occupancy signal to parking decision."><div className="grid gap-4 md:grid-cols-4">{[['1','Detect','A sensor or data source reports whether each space is occupied.'],['2','Store','The backend saves the latest status and timestamp.'],['3','Display','The interface converts statuses into a simple parking-lot map.'],['4','Decide','The driver quickly identifies where available spaces are located.']].map(([n,t,x])=><div key={n} className="rounded-2xl border border-white/10 bg-white/5 p-5"><p className="text-emerald-400">{n}</p><h2 className="mt-2 font-semibold text-white">{t}</h2><p className="mt-2 text-sm leading-6">{x}</p></div>)}</div><h2 className="mt-10 text-2xl font-semibold text-white">MVP scope</h2><p className="mt-3 max-w-3xl">One or more demo parking lots, space-level availability, occupancy totals, simulated real-time updates, dashboard analytics, and a parking assistant that explains availability using the current prototype data.</p></Page>}
