'use client';
import {useState} from 'react'; import Page from '../../components/Page';
export default function Assistant(){const [q,setQ]=useState(''); const [a,setA]=useState(''); function ask(){const s=q.toLowerCase();setA(s.includes('where')||s.includes('space')?'Based on the current demo data, Level 1 has multiple available spaces. Check the Live Parking page for the exact map and latest simulated update.':'I can help explain availability, occupancy, and how Park Now works. I do not reserve spaces or guarantee that a space will remain available.');}return <Page eyebrow="AI Workflow" title="Parking Assistant"><div className="max-w-2xl rounded-3xl border border-white/10 bg-white/5 p-6"><label className="text-sm text-slate-300" htmlFor="q">Ask about parking availability</label><input id="q" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&ask()} placeholder="Where can I park?" className="mt-2 w-full rounded-xl border border-white/10 bg-slate-950 px-4 py-3 text-white"/><button onClick={ask} className="mt-3 rounded-xl bg-emerald-400 px-4 py-2 font-semibold text-slate-950">Ask Assistant</button>{a&&<div className="mt-5 rounded-xl border border-white/10 bg-black/20 p-4 text-sm leading-6">{a}</div>}<p className="mt-4 text-xs text-slate-500">This capstone assistant uses simulated prompt logic, not a paid external AI API. It must not guarantee space availability or encourage unsafe phone use while driving.</p></div></Page>}
